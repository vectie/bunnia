///|
// Seeded shell marker. Keep roles, tabs, onboarding, district gates, and
// presence chips in focused projection_seed_shell_* files so Home and shell
// navigation can evolve without reopening this seed boundary.
