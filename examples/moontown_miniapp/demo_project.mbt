///|
// Moontown generated-project marker. Keep shell view entry points, route
// assembly, scoped runtime patches, and tab metadata in focused demo_project_*
// files so route work can evolve without rebuilding this boundary.
